# 部署指南

1. 配置 `.env`（参考 `.env.example`）
2. 安装后端依赖：`pip install -r backend/requirements.txt`
3. 启动后端：`python backend/app.py`
4. Web 管理端：`cd web-admin && npm install && npm run dev`
5. uni-app：使用 HBuilderX 打开 `mobile-app` 目录运行
